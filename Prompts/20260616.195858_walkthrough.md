# Walkthrough - Save Settings Close Screen

> on clicking 'SAVE SETTINGS', it should also close the screen and go to main screen

## Changes Made

### Settings Screen
- Modified [SettingsActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/02_Stable/TODO_Tasks_List_APK_GITHUB/app/src/main/java/com/vypeensoft/todo/SettingsActivity.java):
  - Added a call to `finish()` in the click listener for `btnSaveSettings` when settings are successfully saved. This terminates the `SettingsActivity` and returns the user to the `MainActivity`.

## Verification
- Added logic verified to close settings screen after saving.
